% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sftp_upload.R
\name{sftp_upload}
\alias{sftp_upload}
\title{Upload a file to an SFTP server}
\usage{
sftp_upload(
  sftp_conn,
  local_file,
  remote_file = NULL,
  .create_dir = FALSE,
  .verbose = TRUE
)
}
\arguments{
\item{sftp_conn}{An \code{SFTPConn} object containing connection details and
authentication. Created by \code{\link{sftp_connect}}.}

\item{local_file}{Character string (path to a file) or a \code{data.frame}.
Data frames are automatically written to a temp file before upload.
The temp file will automatically be cleaned up at the end of function.}

\item{remote_file}{Character string. The destination path on the server.
If \code{NULL}, attempts to use the basename of the \code{local_file}.}

\item{.create_dir}{Logical. Defaults to \code{FALSE}. If \code{TRUE},
creates the necessary parent directories if needed.}

\item{.verbose}{Logical. Defaults to \code{TRUE}.
Prints helpful messages.}
}
\value{
Returns \code{TRUE} (invisibly) on success. Throws an error on failure.
}
\description{
A robust wrapper to upload local files or data frames to a remote SFTP
server. It manages the libcurl handle lifecycle, ensures connections are
closed, and validates URLs against the connection's "Source of Truth".
}
\details{
The function uses a secure lifecycle:
\enumerate{
  \item Validates the remote URL to prevent credential leakage.
  \item Opens a file connection to the local source.
  \item Uses \code{on.exit} to ensure file handles are released and
        temporary files are unlinked even if the transfer is interrupted.
}
}
\examples{
\donttest{
if (interactive() || Sys.getenv("R_SFTP_TEST_SERVER") == "true") {
  # Create new SFTP connection
  sftp_conn <- sftp_connect(
    hostname = "127.0.0.1",
    port     = "2222",
    user     = "tester",
    password = "password123"
  )

  # Upload `my_df` as csv
  sftp_upload(conn, my_df, "uploads/data.csv")
}
}
}
