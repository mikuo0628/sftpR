% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sftp_connect.R
\name{sftp_conn_generator}
\alias{sftp_conn_generator}
\title{SFTP Connection Class}
\value{
\code{SFTPConn} R6 class object, used in \code{sftp_*} family.
}
\description{
An R6 class to safely store information needed for SFTP connection,
with convenient methods to check connections and existence of files or
directories, and create specific handles for \code{sftp_*} function family of
CRUD operations.
}
\details{
One important goal of this design choice is to keep user credentials
safe, as private fields. Credentails are used to create specific handles
for \code{sftp_*} family, and are reused where approrpiate. \code{SFTPConn}
This class checks if credential is valid, and has a internal convenience
methods such as safe printing for basic information, checking destination
existence, and ensuring URL is correctly formatted.
}
\note{
This method uses a 5-second \code{connecttimeout} to ensure
  the probe doesn't hang on unresponsive servers.
}
\keyword{internal}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{protocol}}{The connection protocol.}

\item{\code{hostname}}{The server address or IP.}

\item{\code{path}}{The target subdirectory on the server.}

\item{\code{port}}{The port number.}

\item{\code{timeout}}{Connection timeout in seconds.}

\item{\code{h}}{The internal curl handle used for connection checks, listing
directories, and download files.}

\item{\code{.verbose}}{Logical; if TRUE, prints detailed curl output.}

\item{\code{last_error}}{Character string of the last connection error.}
}
\if{html}{\out{</div>}}
}
\section{Active bindings}{
\if{html}{\out{<div class="r6-active-bindings">}}
\describe{
\item{\code{clean_url}}{Returns the processed SFTP URL via internal
\code{.build_sftp_url}.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-SFTPConn-new}{\code{sftp_conn_generator$new()}}
\item \href{#method-SFTPConn-connection_ok}{\code{sftp_conn_generator$connection_ok()}}
\item \href{#method-SFTPConn-print}{\code{sftp_conn_generator$print()}}
\item \href{#method-SFTPConn-.upload_handle}{\code{sftp_conn_generator$.upload_handle()}}
\item \href{#method-SFTPConn-.quote_handle}{\code{sftp_conn_generator$.quote_handle()}}
\item \href{#method-SFTPConn-.exists}{\code{sftp_conn_generator$.exists()}}
\item \href{#method-SFTPConn-.fix_url_type}{\code{sftp_conn_generator$.fix_url_type()}}
\item \href{#method-SFTPConn-clone}{\code{sftp_conn_generator$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-new"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-new}{}}}
\subsection{Method \code{new()}}{
Initialize `SFTPConn` class R6 object.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$new(
  protocol = "sftp",
  hostname = "localhost",
  path = NULL,
  port = "22",
  user = NA_character_,
  password = NA_character_,
  timeout = 30L,
  ...,
  .verbose = TRUE
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{protocol}}{Character. Protocol string. Defaults to "sftp".}

\item{\code{hostname}}{Character. Server URL or IP. Defaults to "localhost".}

\item{\code{path}}{Character. Sub-path on server.}

\item{\code{port}}{Character. Port number. Defaults to "22".}

\item{\code{user}}{Character. SFTP account name.}

\item{\code{password}}{Character. SFTP password.}

\item{\code{timeout}}{Integer. Connection timeout.}

\item{\code{...}}{Additional arguments passed to \code{curl::handle_setopt()}.}

\item{\code{.verbose}}{Logical. Defaults to \code{TRUE}.
Prints helpful messages.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
An `SFTPConn` object with safely stored user credential and
 convenience methods for various operations, such as checking connection
 and existence, and creating handles for CRUD operations.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-connection_ok"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-connection_ok}{}}}
\subsection{Method \code{connection_ok()}}{
Checks if the current connection settings and credentials are valid.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$connection_ok()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
Logical; TRUE if connection is successful.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-print"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-print}{}}}
\subsection{Method \code{print()}}{
Custom print method to display connection status without exposing
passwords.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$print(...)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{...}}{Unused.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-.upload_handle"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-.upload_handle}{}}}
\subsection{Method \code{.upload_handle()}}{
Internal method to generate a specialized upload handle with streaming.
Adapted from \code{curl::curl_upload()}.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$.upload_handle(
  local_file,
  reuse = TRUE,
  .verbose = self$.verbose,
  ...
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{local_file}}{Path to file, data.frame, or connection.}

\item{\code{reuse}}{Logical; try to keep connection alive.}

\item{\code{.verbose}}{Logical. Defaults to \code{TRUE}.
Prints helpful messages.}

\item{\code{...}}{Additional options for \code{curl::handle_setopt()}.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-.quote_handle"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-.quote_handle}{}}}
\subsection{Method \code{.quote_handle()}}{
Createshandle that uses `quote` option. Specifically for deleting,
creating directories, and renaming files or directories.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$.quote_handle(
  remote_url_from = NULL,
  remote_url_to = NULL,
  purpose = c("rm", "mkdir", "rename"),
  .verbose = self$.verbose,
  .ignore_error = FALSE,
  ...
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{remote_url_from}}{Character. The URL to delete, to create, or to
rename from.}

\item{\code{remote_url_to}}{Character. The URL to rename to. Ignored for delete
or directory create operations.}

\item{\code{purpose}}{Character. Choose one of 3 options:
\itemize{
   \item "rm": to delete file or directory.
   \item "mkdir": to create directory. Path should be a directory.
   \item "rename": to rename
}}

\item{\code{.verbose}}{Logical. Defaults to \code{TRUE}.
Prints helpful messages.}

\item{\code{.ignore_error}}{Logical. Defaults to `FALSE`.
If \code{TRUE}, error will not interrupt subsequent execution.
See `Details`.}

\item{\code{...}}{Options that for \code{curl::handle_setopt()}.}
}
\if{html}{\out{</div>}}
}
\subsection{Details}{
In `curl`, adding an asterisk (`*`) at the very  beginning of a
  command (ie. one of the 3 used in `purpose` argument)
  acts as a "fail-safe" or "ignore-error" prefix. It silently ignores
  any failure returned by the command, and continues without being
  interrupted by the error.
Check if a remote path exists
}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-.exists"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-.exists}{}}}
\subsection{Method \code{.exists()}}{
An internal helper that pings the SFTP/FTP server to verify
the existence of a file or directory.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$.exists(sftp_url = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{sftp_url}}{Character. The full URL to the remote resource.
If \code{NULL}, returns \code{FALSE}.}
}
\if{html}{\out{</div>}}
}
\subsection{Details}{
This method uses \code{CURLOPT_NOBODY = TRUE} to perform a
protocol-level \code{STAT} request. This is highly efficient
as it retrieves only metadata and does not attempt to download or
list contents.

Because \code{STAT} is slash-agnostic in the SFTP protocol, this check
will return \code{TRUE} for a directory regardless of whether a trailing
forward-slash is provided in the URL.

However, this is the only operation where URL is "safe" from the
consequences of un-normalized URLs. Be wary of incorrect multiple
slash placements as they will be collapsed into one slash and won't
throw errors.
}

\subsection{Returns}{
Logical. \code{TRUE} if the resource exists and is accessible;
  \code{FALSE} otherwise.
URL "fixing" via range probing
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-.fix_url_type"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-.fix_url_type}{}}}
\subsection{Method \code{.fix_url_type()}}{
An internal diagnostic method that determines if a remote URL requires a
trailing slash by attempting to read a single byte (Range: 0-0).
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$.fix_url_type(remote_url)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{remote_url}}{Character. The full SFTP/FTP URL to validate.}
}
\if{html}{\out{</div>}}
}
\subsection{Details}{
This method leverages a protocol behavior:
\itemize{
  \item \strong{Files} allow byte-range requests; the probe succeeds.
  \item \strong{Directories} reject byte-range requests; the probe fails.
}

If the initial probe fails, the method "flips" the trailing slash
  (adds one if missing, or removes one if present) and returns the
  modified URL. This addresses the common `libcurl` issue where directory
  listings fail without an explicit trailing slash.
}

\subsection{Returns}{
A character string containing the "fixed" URL. Note that if the
  path truly does not exist, the flipped URL is still returned; the final
  operation (upload/list) will handle the ultimate failure.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SFTPConn-clone"></a>}}
\if{latex}{\out{\hypertarget{method-SFTPConn-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{sftp_conn_generator$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
